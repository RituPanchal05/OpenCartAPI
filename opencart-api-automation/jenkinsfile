pipeline {
    agent any

    

   
    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/RituPanchal05/OpenCartAPI.git'  // Replace with your repo
            }
        }

        stage('Build & Test') {
            steps {
                sh 'mvn clean test'
            }
        }

        stage('Allure Report') {
            steps {
                allure includeProperties: false, jdk: '', results: [[path: "${env.REPORT_DIR}"]]
            }
        }
    }

    post {
        always {
            junit 'target/surefire-reports/*.xml'
            echo 'Build completed!'
        }
    }
}
